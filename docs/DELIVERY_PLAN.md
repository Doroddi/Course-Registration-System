# 변경 단위와 검증 계획

이 문서는 요구사항을 PR과 검증 결과에 연결하기 위한 계획이다. 완료 여부는 각 PR의 실제 변경 및 실행 결과로 판단하며, 아래 순서는 새 정책의 확정을 의미하지 않는다.

## PR 구성

| 순서 | 변경 단위 | 포함 범위 | 완료 기준 |
|---|---|---|---|
| 1 | 초기 설계 | 기존 [PR #1](https://github.com/Doroddi/Course-Registration-System/pull/1)의 요구사항·논리 모델·초기 골격 | 설계 검토 의견과 반영 여부 기록 |
| 2 | API 및 상세 설계 | 7개 API 명세, 요구사항, 데이터 모델, 동시성 정책, 기술 선택, 관련 프롬프트 | 정책·명세·검증 시나리오 대조, 미정 사항 구분, 문서 링크 확인 |
| 3 | 실행 환경 | Spring Boot, Gradle Wrapper, Compose, 환경 변수 예제, 기동 테스트, 실행 문서 | 빌드·테스트·독립 JAR 실행 및 PostgreSQL 컨테이너 접속 확인 |
| 4 | 스키마와 JPA | Flyway DDL, 엔티티·키·관계·삭제 제한 | 실제 PostgreSQL에서 마이그레이션, ddl-auto=validate, 제약조건 검증 |
| 5 | 초기 데이터와 준비 상태 | 요구 규모의 데이터 생성, 대상 학기 설정, 데이터 완전성, /health | 생성 시간·개수 측정, 준비 전후 응답, 재시작 동작 검증 |
| 6 | 인증 | 모의 계정, 비밀번호 검증, JWT 발급·검증, 접근 제한 | 로그인 성공·실패, 누락·위조·만료 토큰, 본인 식별 검증 |
| 7 | 목록 조회 | 공통 입력 검증·페이지 응답을 먼저 구현한 뒤 강좌·학생·교수의 필터·정렬·집계 연결 | 공통 코드의 D90~D91·T122~T125 검증 후 실제 목록의 정상·빈 결과·입력 오류·인증 조건, 중복 집계와 조회 SQL 확인 |
| 8 | 신청·취소·시간표 | 학생→강좌 잠금, 조건 순서, 삭제·재신청, 신청 순서 조회 | 순차 동작과 기본 경합 테스트, 커밋 후 정원·학점·중복·시간 충돌 확인 |
| 9 | 동시성 및 최종 검증 | 경합 확대, 잠금 모드·대기 실패·롤백, 실행·API 문서 | 요구사항별 테스트 근거 연결, 측정 조건·한계·재현 절차 기록 |

신청·취소·시간표 구현이 커지면 PR을 나누되, 공통 잠금 규칙을 함께 검토한다. 동시성 테스트는 마지막 단계까지 미루지 않고 해당 기능 PR부터 포함한다.

## 설계 PR과 환경 PR의 경계

- 설계 PR은 [본문 초안](reviews/api-design-pr.md)에 나열한 문서를 포함한다.
- README는 설계 문서 링크 변경과 실행 안내 변경을 나누어 반영한다.
- 기술 스택 문서의 선택 근거는 설계 PR에, 실제 빌드·실행 결과와 BOOTSTRAP 링크는 환경 PR에 포함한다. 선행 PR에서 아직 없는 파일을 참조하지 않도록 확인한다.
- 환경 PR에는 build.gradle.kts, Gradle Wrapper와 실행 스크립트, src의 기존 진입점 교체·새 서버·설정·테스트, compose.yaml, .env.example, .gitignore, BOOTSTRAP.md를 포함한다.
- AGENTS.md의 협업 범위 변경도 설계·기록 규칙 변경으로 별도 확인한다. 비밀 값이 들어간 .env 및 로컬 도구·빌드 산출물은 게시하지 않는다.
- 초기 설계가 미병합이면 후속 설계 PR은 initial-design을 기준으로 비교하고 의존 PR을 명시한다. 선행 PR 병합 후 기준 브랜치와 diff를 다시 확인한다. 초기 설계 PR에 후속 구현을 추가하지 않는다.

## 누락 방지 기준

- [요구사항](REQUIREMENTS.md)의 F01~F07, I01~I05, D01~D93, T01~T132를 기준으로 후속 PR에 관련 ID와 실제 테스트를 연결한다. 테스트 계획만 있는 항목을 통과로 표시하지 않는다.
- 인증은 추가 기능이지만 강좌·학생·교수 조회와 신청·취소·시간표의 선행 조건이므로 기능 완료 기준에 포함한다.
- 초기 데이터 규모·생성 제한 시간·건강 상태 응답을 API 구현과 함께 검증한다. 컨테이너 healthy와 서비스 준비 완료를 구분한다.
- 요구사항의 미정 목록은 관련 구현 전에 검토한다. 특히 대상 학기 설정 오류, 데이터 재생성, 계정·JWT 키·재발급, 물리 타입·인덱스, 잠금 대기 설정을 누락하지 않는다.
- 자동 재시도, 신청 기간·학년별 제한, 교수 시간 충돌 등 후속 검토 항목을 임의로 구현 범위에 넣거나 완료 처리하지 않는다.
- 각 PR은 변경 이유, 관련 정책, 실제 검증 결과, 남은 한계, 프롬프트 기록, AI 리뷰 및 사람 리뷰의 반영 결과를 구분한다.
- 핵심 기능은 작성자가 직접 구현하고 AI는 개념·완료 기준 안내와 리뷰를 지원한다. 기존 AI 작성 기반 코드도 실제 지원 범위를 기록하며 작성 주체를 바꾸어 표현하지 않는다.

## 스키마·JPA 구현 검토 상태

2026-09-19: 8개 테이블의 Flyway V1~V9와 엔티티·Repository 구현, PostgreSQL Repository 테스트 130개 통과를 확인했다. [PR 본문 초안](reviews/schema-jpa-pr.md), [AI 자체 리뷰](reviews/schema-jpa-ai-review.md), [실행 결과와 한계](DB_SETUP.md)를 작성했다. [스키마 PR #4](https://github.com/Doroddi/Course-Registration-System/pull/4)를 게시하고 선행 환경 브랜치 대비 45개 파일의 diff·소스 일치·문서 링크를 확인했다. 사람 리뷰는 아직 받지 않았다. PR #2 → #3 → #4 순서로 검토하며 선행 PR 병합 후 기준 브랜치를 다시 확인한다. 다음 구현 단위는 초기 데이터와 준비 상태이다.

## 초기 데이터·준비 상태의 검증 기록 계획

초기 데이터 PR에서는 아래 측정 조건과 결과를 함께 남긴다. 현재 구현과 실행 결과는 INITIAL_DATA.md와 INITIAL_DATA_PERFORMANCE.md를 기준으로 확인한다. 아래 항목 중 미구현 업무 API의 준비 확인은 최종 검증까지 남아 있다.

- 실행한 커밋, JDK·DB 버전, 실행 명령, 측정 환경과 DB 초기 상태를 명시한다.
- 학과 10개·강좌 500개·학생 10,000명·교수 100명 이상의 실제 생성 개수를 조회해 확인한다. 강좌별 수업 시간·담당 교수와 참조 관계의 완전성도 검사한다.
- 서버 프로세스 시작부터 초기 데이터 및 API 준비가 끝나 /health가 200을 반환하기까지의 시간을 측정한다. 데이터 생성 구간의 소요 시간과 구분하고, 1분 이내 조건 충족 여부를 기록한다.
- 준비 전후의 /health 응답과 준비 완료 후 사용 가능한 API 호출 결과를 기록한다. API가 아직 구현되지 않은 단계에서는 전체 준비 조건을 충족했다고 표시하지 않는다.
- 빈 DB 최초 실행과 데이터가 남아 있는 DB 재시작을 구분하여 중복 생성·실패 여부를 확인한다. 실행 횟수와 측정값을 함께 남기고 단일 실행 결과를 일반적인 성능으로 표현하지 않는다.

## 초기 데이터 구현 검토 상태

2026-09-20: V10 학과 코드, 초기 데이터 동적 생성·검증, 세션 배치 저장, 대상 학기 설정과 /health를 구현했다. 전체 테스트 204개와 bootJar 빌드를 통과했다. 초기화 비활성화 시 준비 완료를 선언하지 않으며 기동 테스트도 개발 DB와 분리했다. 실제 JAR 검증은 [측정 기록](INITIAL_DATA_PERFORMANCE.md), 검토 내용은 [AI 자체 리뷰](reviews/initial-data-ai-review.md)를 참조한다. 다음 기능 단위는 로그인·JWT 인증이다.

## 로그인·JWT 인증 구현 검토 상태

2026-09-20: POST /auth/login의 엄격한 JSON 입력 검증·bcrypt 대조·HS256 JWT 발급, Bearer 서명·클레임·정확한 만료 검증과 보호 경로 인증을 구현했다. 전체 300개 테스트가 통과했으며 실패·오류·건너뜀 0, bootJar 성공이다. 인증 관련 96개 중 실제 HTTP·PostgreSQL 통합 테스트는 73개이다. 추가 AI 검토에서 발견한 bcrypt 바이트 경계를 보완한 뒤 인증 102개(HTTP·PostgreSQL 75개)를 재검증해 통과했고 bootJar도 성공했다. 실행 JAR에 테스트용 경로·설정이 포함되지 않았음을 확인했다.

계약·흐름도·검증 범위는 [인증 문서](AUTH_API.md), 변경 범위는 [PR 설명](reviews/auth-pr.md), 검토는 [AI 리뷰](reviews/auth-ai-review.md)를 따른다. [인증 PR #6](https://github.com/Doroddi/Course-Registration-System/pull/6)을 게시했고 초기 데이터 브랜치 기준 39개 파일의 변경 범위를 확인했다. 검토·병합 순서는 PR #2 → #3 → #4 → #5 → #6이며 선행 PR 병합 후 기준 브랜치를 다시 확인한다. 사람 리뷰는 아직 받지 않았다. 후속 목록 조회는 공통 입력 검증·페이지 응답부터 진행하며 재발급·로그아웃은 별도 설계로 남긴다.

인증 실패 진단 보완에서는 외부 누락·만료·일반 오류 응답과 내부 사유·요청 ID 로그를 분리했다. 인증 테스트 157개와 bootJar가 통과했으며 결과와 범위는 [인증 실패 진단 검증](AUTH_API.md#인증-실패-진단-검증)을 따른다.

## 목록 공통 입력·페이지 응답의 구현 범위

2026-09-20: D90~D91의 숫자 형식·범위·offset·오류 우선순위를 확정했다. ListQueryParser, ListQuery, PageResponse와 해당 공통 코드 테스트를 구현 범위로 한다. 공통 파서는 DB에 접근하지 않고 기존 InvalidParameterException 처리기를 재사용한다. 역할과 공식 기술 근거는 [공통 구현](LIST_QUERY_COMMON.md)에 정리한다.

T122~T125에 대응하는 공통 테스트 68개(파서 단위 57개, standalone MockMvc 바인딩·오류 응답 5개, PageResponse 6개)가 통과했다. 실패·오류·건너뜀 0이며 bootJar도 성공했다. 실행 명령·재검증 범위와 JAR 포함 여부는 [공통 구현](LIST_QUERY_COMMON.md)을 따른다. 이 공통 코드 검증은 실제 목록 API·DB 검증을 대신하지 않는다. 후속 학생·교수 구현은 아래에 구분하며, 강좌 목록의 격리 수준·배열 정렬은 D93으로 확정했고 강좌 코드는 D18의 숫자 크기순으로 비교한다.

## 학생·교수 목록 구현 검토 상태

2026-09-22: GET /students·GET /professors의 DTO·Repository·QueryService·Controller와 학과 부재 404를 연결했다. 학생은 학과·학년 필터와 학번 정렬, 교수는 소속 학과 필터와 ID 정렬을 적용한다. D92에 따라 읽기 전용 READ COMMITTED를 사용하고 명시적 행 잠금은 사용하지 않는다.

실제 HTTP·PostgreSQL 목록 테스트 40개, 공통 테스트 68개, 인증·JWT 설정 회귀 테스트 157개로 총 265개가 통과했다. 실패·오류·건너뜀 0, bootJar 성공이며 실행 JAR의 테스트 코드 제외를 확인했다. 쿼리 횟수·입력 오류 시 DB 접근 없음 등 T126~T128의 검증 범위와 한계는 [목록 구현·검증](LIST_QUERY_COMMON.md#학생교수-목록-연결)을 따른다. 다음 구현 단위는 강좌 목록이며 조회 일관성과 내부 배열 정렬은 D93으로 확정했다. 강좌 코드는 D18의 숫자 크기순으로 비교한다. 후속 구현 결과는 아래에 구분한다.

## 강좌 목록 구현 검토 상태

2026-09-23: GET /course-offerings의 서버 대상 학기·학과 필터, 숫자 코드 정렬, 페이지 응답, 시간·교수 배열 및 신청 인원 집계를 연결했다. D18·D93과 T129~T132를 반영했다. 코드 정렬은 numeric 변환과 개설 강좌 ID 보조 정렬을 사용하며 관계별 일괄 조회로 다건 조인의 중복을 피한다.

강좌 HTTP·PostgreSQL 통합 테스트 19개를 포함한 목록·인증 테스트 총 284개가 통과했다. 실패·오류·건너뜀 0, bootJar 성공이며 [강좌 API](API.md#실행-결과와-한계)에 쿼리 횟수와 검증 범위·한계를 기록했다. 학생·교수·강좌 목록 기능이 연결됐으며 다음 업무 구현 단위는 수강신청이다. 신청 트랜잭션·잠금·동시성 검증은 별도로 수행한다.

2026-09-24: 목록 조회 변경을 main 기준 PR로 묶는다. [AI 자체 검토](reviews/list-queries-ai-review.md)에서 코드·명세·검증 근거를 대조했고 게시 전 수정이 필요한 기능 오류는 발견하지 못했다. 검증 당시 소스와 현재 소스가 동일함을 확인했으며 이번 검토에서 테스트를 반복 실행하지 않았다. 사람 리뷰는 아직 받지 않았다.

## 수강신청 구현 검토 상태

2026-09-25: POST /enrollments를 비관적 잠금으로 구현했다. 학생 → 강좌 순서, READ COMMITTED, 학생 조건 우선 검사, 잠금 후 별도 정원 COUNT, 3초 잠금 대기와 전체 롤백을 적용했다. 기존 목록 구현 다음의 독립 기능 단위로 관리한다.

신청 통합 테스트 47개·오류 분류 단위 테스트 6개가 통과했고 bootJar 빌드도 성공했다. 실제 잠금 SQL과 실패 후 회복을 포함한 범위는 [신청 구현·검증 기록](ENROLLMENTS_API.md#구현검증-기록)을 따른다. 취소·시간표 조회, 신청·취소 경합, 부하 측정은 남아 있다. 사람 리뷰는 아직 받지 않았다.

전체 회귀 검증: 2026-09-25에 ./gradlew test bootJar --no-daemon을 실행하여 31개 테스트 클래스의 541개 테스트가 통과했다(실패·오류·건너뜀 0). bootJar 성공, 실행 JAR에서 테스트 코드 제외를 확인했다. 종료 단계에서 Hikari 연결 재시도 경고가 출력됐으며 빌드와 테스트는 정상 종료했다.

## 신청 PR과 취소 구현

[신청 PR #9](https://github.com/Doroddi/Course-Registration-System/pull/9)은 목록 조회 PR #8을 기준으로 게시했다. 선행 PR 병합 후 main 기준 변경 범위를 다시 확인한다.

2026-09-25: 취소 API를 신청 PR 이후의 별도 변경으로 구현했다. 본인 신청 삭제·반복 취소 204, 대상 학기 제한, 학생 → 강좌 잠금과 실패 롤백을 적용했다. 신청·취소 관련 테스트 80개(통합 74개·단위 6개), bootJar가 통과했다. 검증 범위와 이전 전체 테스트 결과의 구분은 [취소 명세·검증](CANCELLATIONS_API.md)을 따른다. 시간표 API와 부하 측정이 남아 있다. 사람 리뷰는 아직 받지 않았다.

## 취소 PR과 시간표 구현

[취소 PR #10](https://github.com/Doroddi/Course-Registration-System/pull/10)은 신청 PR #9를 기준으로 게시했다. 선행 변경을 포함하지 않도록 #8 → #9 → #10 순서로 검토하고, 선행 PR 병합 후 기준 브랜치를 다시 확인한다.

2026-09-25: GET /me/timetable을 구현했다. JWT 본인·대상 학기의 신청 ID순 목록, 수업 시간·교수 일괄 조회, 동일 목록의 학점 합을 반환한다. 시간표 18개와 신청·취소 80개, 총 98개 테스트 및 bootJar가 통과했다. [시간표 구현·검증](TIMETABLE_API.md)에 쿼리 구성과 동시 변경 시 일관성 검증을 기록했다. 후속 범위는 전체 요구사항 대조, 전체 회귀·실행 준비 상태 및 부하 검증이다. 사람 리뷰는 아직 받지 않았다.

## 시간표 PR과 통합 검증

[시간표 PR #11](https://github.com/Doroddi/Course-Registration-System/pull/11)을 취소 PR #10 기준으로 게시했다. D72의 읽기 전용 READ COMMITTED·잠금 없는 관계별 조회와 최초 목록 기준 학점 합을 확정 기록했다.

후속 검증에서는 원 과제의 100명 마지막 자리 경쟁, 실행 JAR의 /health 준비 직후 업무 API 사용, 정상 재시작 보존과 불완전 데이터 기동 실패를 확인한다. 테스트 실행 결과와 요구사항별 근거는 [통합 검증 기록](FINAL_VERIFICATION.md)에 구분한다.

2026-09-25 통합 검증 완료: 전체 587개 테스트(32개 클래스), 실패·오류·건너뜀 0, bootJar 성공. 100명 마지막 자리 경쟁은 성공 1건·COURSE_FULL 99건·최종 1건이었다. 실행 JAR 최초 준비 25.657초·재시작 18.449초, 최초 준비 후 모든 업무 API 호출 및 불완전 DB 기동 실패·데이터 보존을 확인했다. 단일 환경 관측이며 대규모 지속 부하의 성능 보장과 구분한다.

## HTTP 부하 기준값

2026-09-25: 운영 코드와 잠금·연결 풀 설정을 유지하고 집중·분산·조회 혼합을 각각 동시 사용자 10·50·100명으로 측정했다. 조건별 준비 10초·본 측정 30초의 첫 탐색 실행이며 전체 27,355건에서 503·예상 밖 응답·전송 오류는 0건, COURSE_FULL은 1,431건이었다. 재현 스크립트·환경·성공/거절별 지연·해석 한계는 [부하 기준값](LOAD_BASELINE.md)에 기록했다. 장시간 부하와 반복 편차, 잠금·연결 풀 압력 계측 및 성능 목표 결정은 후속 범위다. [AI 자체 검토](reviews/final-verification-ai-review.md)와 사람 리뷰를 구분한다.

2026-09-25 병합 상태: 목록 조회 #8, 신청 #9, 취소 #10, 시간표 #11을 순서대로 main에 병합했다. 병합된 소스 트리가 기존 시간표 구현 커밋과 동일함을 확인했다. 사람 리뷰 완료를 의미하지 않으며, 이번 통합 검증·HTTP 부하 기준값은 별도 PR로 관리한다.

2026-09-25 후속 계측: 정원 30명에 200명 단발 신청을 통합 테스트와 k6에서 각각 검증했다. 두 실행 모두 성공 30·COURSE_FULL 170·DB 고유 학생 30이었다. 100명 집중·분산·혼합에서 연결 풀·DB 잠금·쿼리·자원을 수집했다. 연결 확보 대기와 높은 시스템 CPU 사용을 관측했으며, 계측 비용과 환경 편차를 구분하기 위해 풀·잠금 설정은 유지했다. 결과와 재현 방법은 [부하 진단 기록](LOAD_DIAGNOSTICS.md)을 따른다.

후속 계측 변경 전체 회귀: 33개 클래스·590개 테스트 통과(실패·오류·건너뜀 0), bootJar 성공. 진단 포트 인증·격리와 기본 실행의 지표 비노출을 검증했다.

2026-09-26 반복 계측: 같은 JAR·100명 조건으로 두 번 추가 측정했다. 연결 대기자 최대 89~90개와 부하 구간 호스트 CPU 평균 약 93~99%를 관측했다. 모든 조건의 DB 불변식 검사가 통과했지만 응답시간 편차가 커서 풀 크기·잠금 정책은 유지했다. [반복 결과와 해석 한계](LOAD_DIAGNOSTICS.md)에 근거를 기록했다. 다음 성능 비교는 부하 생성기 분리 또는 다른 작업 부하 통제 후 진행한다.

2026-09-26 저부하 확인: 10명 집중·분산·혼합 1회에서 5,114건 정상 응답과 DB 불변식 통과를 확인했다. 연결 확보 평균은 0.10~0.17ms였지만 호스트 CPU 평균이 약 95%여서 50명 확대와 추가 반복을 중단했다. 계획한 3회 반복의 완료나 환경 통제를 주장하지 않는다. 설정은 유지하며 상세 근거는 [10명 조건 확인](LOAD_DIAGNOSTICS.md)을 따른다.

2026-09-26 마무리: 로컬 성능 추가 측정은 종료한다. 정합성 검증과 성능 관측의 한계를 유지한 채 진단 변경을 PR로 정리한다. 핵심 API·실행·초기 데이터·동시성 요구를 다시 대조했고, README 및 통합 검증 문서의 오래된 상태 설명을 갱신했다. 사람 리뷰와 PR 병합은 별도 절차다.
